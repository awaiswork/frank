services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: frank
      POSTGRES_PASSWORD: frank
      POSTGRES_DB: frank
    ports:
      # Host 5433 (not 5432) to avoid colliding with a local Postgres install.
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U frank -d frank"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  pgdata:
